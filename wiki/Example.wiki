#summary Example Source
#labels Featured,ExampleSource

=== - Read Xml Url ===

URL url;
try {
    url = new URL("http://url/test.xml");
    URLConnection uc = url.openConnection();
    Document doc = new XmlParser().parse(uc.getInputStream());
    Element ele = doc.gRootElem();
} catch (Exception e) {
}

{{{
def fib(n):
  if n == 0 or n == 1:
    return n
  else:
    # This recursion is not good for large numbers.
    return fib(n-1) + fib(n-2)
}}}